# Reranking 組交接說明

這份文件是給下一個流程，也就是 reranking 負責組員使用。RAG 組目前會輸出一個 candidate pool，reranker 的任務是讀取這個 pool，根據 task validity、branch fidelity、association use 等欄位設計排序策略，選出最佳 candidate。

## 1. 先確認資料與 request

若尚未下載或處理資料，先執行：

```bash
bash scripts/download_data.sh
python scripts/sample_writingbench.py
python scripts/inspect_gutenberg.py
```

產生 request：

```bash
python scripts/build_tot_requests.py
```

目前 `scripts/build_tot_requests.py` 預設：

```python
TARGET_INDEX = 180
BRANCH_COUNT = 3
```

所以會從 `data/test_set/test_set_lit_arts_en.jsonl` 選出 `index=180` 這一題，產生：

```text
data/runs/{index}_request_{timestamp}.jsonl
```

## 2. 產生 reranker candidate pool

Reranking 組主要要吃這個輸出。請執行：

```bash
python scripts/build_reranker_pool_hf.py
```

預設會讀取 `data/runs/` 裡最新的 `*_request_*.jsonl`。

這個 script 會依序做：

```text
constraint_map
→ expand_branches
→ 對每個 branch 套用 RAG variants
→ draft candidate
→ judge candidate
→ 計算 derived_metrics
→ 輸出 candidate pool JSONL
```

輸出檔案格式：

```text
data/runs/{index}_reranker_pool_hf_{timestamp}.jsonl
```

目前已測試產生過：

```text
data/runs/180_reranker_pool_hf_20260531_175334.jsonl
```

## 3. Candidate pool 的內容

每一行是一個 candidate。以預設設定來說：

```text
4 個 RAG variants × 3 個 branches = 12 個 candidates
```

四個 RAG variants：

```text
none
random_association
near_relevance
creative_association
```

三個 branches 是 `expand_branches` 產生的三條創作路線。以 index 180 為例：

```text
A = 穩健線性求生版
B = 配送任務 episodic 旅程版
C = 非線性人性重生 / 主題深化版
```

每個 candidate row 的主要欄位：

```json
{
  "index": 180,
  "pool_variant": "creative_association",
  "branch_id": "C",
  "branch_plan": {},
  "association_packet": {},
  "candidate": {},
  "judgment": {},
  "derived_metrics": {},
  "association_use": {},
  "status": "drafted"
}
```

欄位用途：

| 欄位 | 說明 |
|---|---|
| `pool_variant` | RAG variant，例如 `none`、`near_relevance`、`creative_association` |
| `branch_id` | branch 編號，例如 A/B/C |
| `branch_plan` | 該 branch 的敘事策略 |
| `association_packet` | 該 candidate 使用的 RAG knowledge |
| `candidate` | 生成出的候選答案 |
| `judgment` | LLM judge 對 WritingBench checklist、branch fidelity、constraint violations 的評估 |
| `derived_metrics` | 程式從 judgment 推導出的排序用 metrics |
| `association_use` | judge 對 RAG knowledge 是否自然轉換進文本的評分 |
| `status` | `drafted` 表示可排序；若為 `draft_failed`，代表該候選生成失敗 |

## 4. Reranker 組的任務

Reranker 組不需要重新跑 RAG，也不需要重新生成 candidate。主要任務是：

```text
input: data/runs/{index}_reranker_pool_hf_{timestamp}.jsonl
output: reranked candidates / selected candidate
```

建議新增一個 script，例如：

```text
scripts/rerank_candidate_pool.py
```

這個 script 建議做：

```text
1. 讀取 reranker pool JSONL
2. 過濾 status != "drafted" 的 row
3. 使用 derived_metrics 做 task-validity 排序
4. 視需求加入 association_use.score_1_to_5
5. 輸出完整 ranking
6. 標記 selected candidate
```

建議輸出：

```text
data/runs/{index}_reranker_result_{timestamp}.jsonl
```

輸出格式可參考：

```json
{
  "index": 180,
  "selected": {
    "pool_variant": "creative_association",
    "branch_id": "C",
    "candidate": {},
    "judgment": {},
    "derived_metrics": {},
    "association_use": {}
  },
  "ranking": [
    {
      "rank": 1,
      "pool_variant": "creative_association",
      "branch_id": "C",
      "score": 0.0,
      "reason": "..."
    }
  ]
}
```

## 5. 建議排序依據

目前已有的 deterministic metrics 在 `derived_metrics`：

```json
{
  "mean_score": 0.0,
  "min_score": 0.0,
  "branch_fidelity": 0.0,
  "constraint_violation_count": 0,
  "fixable_issue_count": 0,
  "fixable_issues": []
}
```

原本 ToT rerank 的 task-validity 排序邏輯是：

```text
constraint_violation_count 越少越好
min_score 越高越好
mean_score 越高越好
branch_fidelity 越高越好
fixable_issue_count 越少越好
```

RAG 組額外提供：

```json
{
  "association_use": {
    "score_1_to_5": 4,
    "used_item_ids": ["geo_seed_bank_svalbard"],
    "evidence": "...",
    "main_issue": "..."
  }
}
```

Reranker 組可以選擇：

```text
方案 A：只使用 task-validity metrics
方案 B：task-validity first，再用 association_use 作 tie-breaker
方案 C：把 association_use 加權進總分
方案 D：先過濾 constraint violation，再在合格候選中排序 creativity / association quality
```

## 6. 注意事項

- `none` 是 no-RAG baseline，不應被視為錯誤。
- `random_association` 是 baseline，用來比較隨機靈感是否真的有幫助。
- `near_relevance` 接近普通 RAG。
- `creative_association` 是 RAG 組主要方法。
- `association_use` 目前只是提供給 reranker 的額外欄位，尚未直接參與排序。
- 如果 `status == "draft_failed"`，該 row 通常不要納入 ranking，但可以保留作錯誤分析。
- RAG 組目前沒有做 final revision；final revision 可以在 reranker 選出 selected candidate 後再接回後處理。

## 7. 最小執行順序

如果資料與 request 都已經存在，最小流程是：

```bash
python scripts/build_reranker_pool_hf.py
# 接著由 reranker 組執行自己的排序 script，例如：
python scripts/rerank_candidate_pool.py
```

如果要重新指定題目，先改 `scripts/build_tot_requests.py` 的：

```python
TARGET_INDEX = 180
```

再重新執行：

```bash
python scripts/build_tot_requests.py
python scripts/build_reranker_pool_hf.py
```
